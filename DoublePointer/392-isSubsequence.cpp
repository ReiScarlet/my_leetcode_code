#include <bits/stdc++.h>

class Solution {
 public:
  bool isSubsequence(std::string s, std::string t) {
    const int& n = s.size();
    int left = 0, right = t.size();
    for (int pos = 0; pos < n; pos++) {
    }
  }
};